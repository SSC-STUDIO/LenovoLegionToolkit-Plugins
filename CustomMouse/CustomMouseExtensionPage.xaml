<UserControl x:Class="LenovoLegionToolkit.Plugins.CustomMouse.CustomMouseExtensionPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    
    <Border Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}" 
            CornerRadius="8" Padding="20" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <TextBlock Grid.Row="0" Text="W11-CC-V2.2-HDPI 鼠标样式" 
                      FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <!-- 状态 -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                <Ellipse x:Name="StatusIndicator" Width="12" Height="12" Fill="Gray" Margin="0,0,8,0"/>
                <TextBlock x:Name="StatusText" Text="未应用" FontSize="14" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- 操作按钮 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
                <Button x:Name="ApplyButton" Content="应用样式" Click="ApplyButton_Click" 
                       Style="{StaticResource AccentButtonStyle}" Margin="0,0,10,0"/>
                <Button x:Name="RemoveButton" Content="移除样式" Click="RemoveButton_Click"/>
            </StackPanel>

            <!-- 说明 -->
            <StackPanel Grid.Row="3">
                <TextBlock Text="功能说明:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock Text="• Windows 11风格高清鼠标光标" FontSize="12" Margin="0,0,0,3"/>
                <TextBlock Text="• 支持浅色和深色主题自动切换" FontSize="12" Margin="0,0,0,3"/>
                <TextBlock Text="• 高DPI显示优化" FontSize="12" Margin="0,0,0,3"/>
                <TextBlock Text="• 多种光标样式选择" FontSize="12"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>